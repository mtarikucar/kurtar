{
  "name": "@kurtar/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-surface Playwright test proving the money loop end to end against the real API + built web surfaces (Task 14). See tests/money-loop.spec.ts and docs/operations.md.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install --with-deps chromium",
    "lint": "eslint \"tests/**/*.ts\" \"*.ts\"",
    "lint:fix": "eslint \"tests/**/*.ts\" \"*.ts\" --fix",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^20.10.6",
    "@types/pg": "^8.11.10",
    "@typescript-eslint/eslint-plugin": "^6.17.0",
    "@typescript-eslint/parser": "^6.17.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.2",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3"
  }
}
