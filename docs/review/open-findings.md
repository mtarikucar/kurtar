# Açık bulgular — güncel main e karşı yeniden yargılanmış

47 bulgu 8 alan ajanı tarafından bugünkü koda karşı yeniden değerlendirildi: **45 hâlâ gerçek**, 2 mükerrer, 2 kendiliğinden kapanmış.

Her hüküm dosya:satır kanıtı taşır. Düzeltme taslakları triage ajanlarının kendi önerileridir.

---

## CRITICAL (2)

### C1 — Extending a HELD batch after a successor's payout was sent orphans the new settlement lines permanently — merchant is never paid for them

- **alan:** settlements · **iş yükü:** medium

**Kanıt:** backend/src/modules/settlements/settlement-batch-builder.service.ts:489 — `await this.prisma.settlementLine.createMany({...})` still runs on the root client, outside any transaction; recomputeBatch opens its OWN `this.prisma.$transaction` at :523 and is invoked separately by the caller at :332-335. createOrExtendBatch still matches HELD batches (:450, `status: { in: [...RECOMPUTABLE_SETTLEMENT_STATUSES] }`). assertIrrevocableClaimsHonoured (:1216-1256) still throws ConflictException `SETTLEMENT_CARRIED_DEMAND_ALREADY_COLLECTED` at :1243, called at :755 inside the recompute transaction, so the whole recompute rolls back while the lines stay committed. No writer can ever remove or re-point a line: `grep 'settlementLine\.(create|update|delete|...)'` over backend/src (non-spec) returns exactly three hits — :489 createMany, :705 and :847 column-only updates. The nightly scan excludes the reservation forever (`settlementLine: null`, :248) and the clawback-only sweep builds for `todayKey` (:363-370), never for A's periodStart. adminApprove (settlements.service.ts:97) and adminRetry (:173) both re-enter recomputeBatch and re-throw the same 409. The branch's own spec encodes the divergence: settlements.realdb.spec.ts:2202-2210 asserts `settlementLine.count === 2` for the batch while `refusedB1.grossCents === 20000`. One correction to the finding: the `bagFeeCentsOverride` variant is not reachable from any API today — grep shows no writer for that column outside pricing.service.ts's read (:60), so it needs a direct DB edit. The late-redemption path (the one the spec builds) is fully reachable and is enough on its own.

**Düzeltme taslağı:** Make the line insert part of the same transaction as the recompute, so a refusal rolls the lines back and the reservations stay `settlementLine: null` for the next cycle. Concretely: have `createOrExtendBatch` stop calling `settlementLine.createMany` and instead return `{ batchId, pendingLines }` (the already-computed perLine rows plus redeemedAt); add an optional `pendingLines` param to `recomputeBatch` which does the `tx.settlementLine.createMany({ ..., skipDuplicates: true })` inside its own transaction, immediately before it reads `lineRows` at :547. The three call sites (:332, :369, and settlements.service.ts's approve/retry which pass none) change trivially. Effort is medium only because the realdb specs that assert the current 'lines committed, batch refused' end state ([n] at ~2200 and [o] at ~2226) must be re-pointed at the new 'nothing committed, retried next cycle' invariant.

### C2 — Commission invoicing is not idempotent while the outbox is at-least-once — a redelivery issues a second real e-fatura for the same batch

- **alan:** settlements · **iş yükü:** medium

**Kanıt:** backend/src/modules/invoicing/commission-invoice.service.ts:147 is still an unconditional `this.prisma.commissionInvoice.create(...)` with no existence check. No unique constraint exists: schema.prisma:1098-1132 declares only `@@map`, and `grep commission_invoices backend/prisma/migrations` shows only the PK (20260812171242_init/migration.sql:391) and the two FKs (:713,:716). The provider idempotency key is `invoice.id` (:163 passes the freshly-minted cuid; mock-e-document-provider.ts:35-37 dedupes on `invoice.invoiceId`), so a re-run can never hit the provider's dedupe. Both redelivery paths are live: a throwing handler is retried (outbox-worker.service.ts:271-293, MAX_OUTBOX_ATTEMPTS backoff), and a succeeded handler whose markDone fails is deliberately left PROCESSING for the stale-lease reclaim (:295-313, and claimBatch:172 re-claims PROCESSING rows past OUTBOX_LEASE_MS). The MEMBERSHIP branch's create (:114→:147) is NOT inside the BAG_FEE try/catch, so a transient failure there propagates out of `createInvoicesForSentBatch` and re-runs the BAG_FEE leg from :86 on the next attempt. The post-issue bookkeeping `update` (:170-177) is still inside the same `try` as `facade.issue` (:162), and the catch at :178-182 still logs 'drafted but issuance failed — left DRAFT' even when issuance succeeded. This is a real e-fatura in production: NilveraAdapter is a real provider selected by `EDOC_PROVIDER` (e-document-facade.service.ts:20-28) and the mock refuses to register when NODE_ENV=production (mock-e-document-provider.ts:29-31).

**Düzeltme taslağı:** Add `@@unique([batchId, type])` to CommissionInvoice plus a reversible up/down migration (the column is nullable, so scope it to non-null batchIds or accept Postgres's NULL-distinct semantics). Then replace the `create` at :147 with an upsert-or-skip on that key: on conflict, load the existing row and, if its status is still DRAFT, re-`facade.issue` with the SAME `invoice.id` — which the provider contract (e-document-provider.interface.ts:17-22) already dedupes — and if it is SENT, return without touching the provider. Separately, move the `commissionInvoice.update` at :170 out of the `issue` try block and give the 'issued at provider, status not recorded' case its own CRITICAL log line distinct from :180.

---

## IMPORTANT (14)

### I1 — A failed commission e-invoice is a permanent silent dead end — no retry, no queue, no alert

- **alan:** settlements · **iş yükü:** small

**Kanıt:** backend/src/modules/invoicing/commission-invoice.service.ts:178-182: the catch around `facade.issue` still logs and deliberately does not rethrow. settlement-sent-invoice.handler.ts:37 awaits it, so `dispatchOne` reaches markDone (outbox-worker.service.ts:315) and marks the event DONE — the retry/backoff/DEAD machinery never engages. The invalid-taxId branch at :71-76 still `return`s without drafting anything, logging only. `grep -rn 'commissionInvoice|CommissionInvoice' backend/src` (non-spec) confirms the only touches are the create at :147, the update at :170, and two read-only `include`s in settlements.service.ts:72 and :216 — there is no retry cron, no admin re-issue endpoint, no DRAFT queue, and no admin-web surface. There is also no dead-letter alerting to fall back on: `grep OPS_ALERT_EMAIL backend/src` returns only complaint-sla-cron.service.ts:197 and moderation-takedown-cron.service.ts:160, and nothing anywhere sweeps or alerts on outbox rows in status DEAD. So a Nilvera outage during the payout window leaves a day of invoices DRAFT with no signal beyond a log line.

**Düzeltme taslağı:** Once the C2 unique constraint + upsert lands (which makes a re-run safe), rethrow from `createAndIssue`'s catch at :182 so the outbox's existing backoff retries the event, and add one alert: either an `OPS_ALERT_EMAIL` digest from a small cron over `commissionInvoice` rows still DRAFT after N hours, or — cheaper and broader — a daily sweep over outbox rows in status DEAD that emails the same address, which also covers every other handler. Keep the invalid-taxId branch (:71-76) non-throwing (retrying cannot fix bad data) but route it into the same alert rather than a bare logger.error. Small only if it lands after C2; rethrowing before the idempotency fix would convert a silent gap into duplicated tax documents.

### I2 — Membership renewal resets outstandingCents while an open batch still holds a reversible claim on it

- **alan:** settlements · **iş yükü:** small

**Kanıt:** backend/src/modules/memberships/membership-offset.service.ts:148-153 still computes `dueCents: exempt ? batchPriorOffsetCents : sub.outstandingCents + batchPriorOffsetCents`. `periodDate` is passed in (:128) but is used ONLY for the exemption comparison at :139-141 — there is no comparison against `sub.currentPeriodStart`/`currentPeriodEnd` anywhere in the method, and the row is read with `SELECT *` at :131-135 so both columns are available and simply unused. membership-renewal-cron.service.ts:220-243 still overwrites `outstandingCents: totalDueCents` unconditionally, with no query for open CALCULATED/HELD batches. The double-charge path is reachable: a CALCULATED batch that straddles the anniversary is recomputed by settlements.service.ts:97 (adminApprove) and a HELD one by :173 (adminRetry) or by any late-line extension, and `persistOffset` (membership-offset.service.ts:250-255) writes `stored + batchPrior - applied`, so period 1's contribution is added on top of period 2's freshly-set balance. The 'worse variant' also holds: the write-off at renewal-cron:194-243 increments `writtenOffCents` and resets outstanding, but nothing marks the batch's prior contribution as belonging to the forgiven period, so a later clamp credits it back onto period 2.

**Düzeltme taslağı:** In `lockAndResolveDue`, add a period guard alongside the existing `exempt` one: `const foreignPeriod = periodDate < sub.currentPeriodStart || periodDate >= sub.currentPeriodEnd;` and make the due a restore-only value when it is true — i.e. change :148-153 to `dueCents: exempt || foreignPeriod ? batchPriorOffsetCents : sub.outstandingCents + batchPriorOffsetCents` (same for dueVatCents), and skip the `periodPaidAt`/activation flags in `persistOffset` for that case exactly as the exempt branch already does at :270-277. That removes the double-charge outright: a straddling batch can only ever restore what it already took, never collect against the new period. It leaves one smaller residual — if such a batch's own availability shrinks so the restore is clamped, `stored + batchPrior - applied` still credits the released kuruş onto the new period's balance rather than the write-off ledger; worth a follow-up note, not a blocker.

### I3 — settlement-reconciliation alerts on a state nothing can ever clear, unbounded and without a deadline

- **alan:** settlements · **iş yükü:** medium

**Kanıt:** backend/src/modules/settlements/settlement-payout.service.ts:256-264: `findMany({ where: { status: 'SENT', sentAt: { lte: staleSentThreshold } } })` — still no `take`, no `orderBy`, no sentinel column, and :265-269 emits one `logger.error('CRITICAL: ...')` per row on every 09:00 tick (@Cron at :246). Nothing ever writes SETTLED: `grep -rn '"SETTLED"' backend/src` (non-spec) returns exactly one hit, settlement-transitions.ts:46's transition map declaring `SENT: ['SETTLED','FAILED']`, and admin-settlements.controller.ts exposes only list/get/approve/hold/retry/run-nightly. So the alert set is monotonically growing and unclearable by construction. The sibling crons do have the discipline this one lacks: complaint-sla-cron.service.ts:8 `BATCH_LIMIT = 500` with `LIMIT ${BATCH_LIMIT}` at :128 and :154, same in moderation-takedown-cron.service.ts:8,115,133. The second branch (:271-282, the real 'past dueAt, SLA missed' alert) is equally unbounded and shares the same CRITICAL level in the same method, so it is the one that gets buried.

**Düzeltme taslağı:** Two independent parts. (1) Bound both queries: add `take: BATCH_LIMIT` (500, matching the siblings) and `orderBy: { sentAt: 'asc' }` / `{ dueAt: 'asc' }` so a backlog is worked oldest-first rather than scanned whole. (2) Stop branch (a) from re-firing forever: either gate it behind a config flag until a real SENT→SETTLED reconciliation feed exists (cheapest, and honest about the missing writer), or add a `reconciliationAlertSentAt` column on SettlementBatch with a reversible migration, stamped by a guarded UPDATE and filtered on in the WHERE, exactly as complaint-sla-cron does. Medium because option two carries a migration; the bounding alone is trivial and should land regardless.

### I4 — The 5-business-day payout deadline is the only regulated clock with no alert channel and no pre-breach warning

- **alan:** settlements · **iş yükü:** medium

**Kanıt:** backend/src/modules/settlements/settlement-payout.service.ts:271-282: the overdue-unsent branch queries only `dueAt: { lte: now }` — no pre-deadline window — and its only output is `this.logger.error(...)` at :279. `grep -rn OPS_ALERT_EMAIL backend/src` returns only complaint-sla-cron.service.ts:197 and moderation-takedown-cron.service.ts:160, confirming the payout SLA is the one regulated clock with no email channel. Crossing dueAt genuinely requires a human: batches are created CALCULATED (settlement-batch-builder.service.ts:459-467) and only adminApprove (settlements.service.ts:106-109) flips them to APPROVED, which is all the payout cron picks up. There is also no sentinel, so this branch re-logs the same batches daily and shares the CRITICAL level with the unclearable stale-SENT branch above it — the two findings compound. Distinct from the reconciliation finding: that one is about branch (a) being unclearable and unbounded, this one is about branch (b) having no channel and no warning window. Correctly not critical — admin-web does render dueAt as a DeadlineBadge, so it is visible to anyone who logs in.

**Düzeltme taslağı:** Give `reconcileStuckBatches` the shape its two siblings already have: add a third query for batches approaching dueAt (e.g. one business day out, reusing business-days.ts's addBusinessDays) with its own sentinel column so it warns once, and send both the warning and the breach set as an `OPS_ALERT_EMAIL` digest via the same helper complaint-sla-cron.service.ts:197-210 uses, degrading to the existing log line when the env var is unset. Then update docs/operations.md:72 — which today describes this cron only as 'flags batches SENT for more than 3 days' — and the 'When a payout fails' runbook section to name the overdue-unsent condition. Shares the sentinel-column migration with the reconciliation finding, so the two are cheaper done together.

### I5 — Money-moving admin endpoints record no acting admin — the only admin mutations in the codebase that don't

- **alan:** settlements · **iş yükü:** small

**Kanıt:** backend/src/modules/settlements/admin-settlements.controller.ts: `runNightly()` (:51), `approve()` (:65), `hold()` (:74) and `retry()` (:83) take only `@Param`/`@Body` — no `@CurrentUser`, and the file does not import the decorator at all. Same in admin-pricing.controller.ts: `schedule()` (:49) takes only `@Body`. `grep -rn CurrentUser backend/src/modules/settlements` returns only settlements.controller.ts:34,47 (the merchant-facing `merchantId` binds). `grep -rln 'auditLog.create' backend/src/modules` returns complaints, offers, merchants, moderation, memberships (SYSTEM actor, renewal write-off at membership-renewal-cron.service.ts:198), stores and ratings — settlements and pricing are absent. The blast radius is real: `adminApprove` (settlements.service.ts:106-109) flips the batch to APPROVED, which is exactly what the payout cron picks up to send money to a merchant IBAN, and `scheduleFuturePricing` changes the per-bag fee for every merchant on the platform. Neither leaves any record of which admin acted.

**Düzeltme taslağı:** Bind `@CurrentUser("id") adminId: string` on the five handlers and thread it into `adminApprove`/`adminHold`/`adminRetry`/`adminRunNightlyCycle`/`scheduleFuturePricing`. In each service, write an `auditLog.create` row inside the same transaction as the state change — for approve, that means moving the guarded `updateMany` at settlements.service.ts:106-109 into a `$transaction` with the audit write so the two cannot diverge; hold/retry/pricing follow the same shape. Use the `{ actorType: 'ADMIN', actorId, action, entity, entityId, diffJson }` shape and the `entity.verb` action naming the other modules already use (`settlement.approved`, `settlement.held`, `settlement.retried`, `settlement.nightly_run`, `pricing.scheduled`). No migration needed — the AuditLog table already exists and is written from seven other modules.

### I6 — Logout never unregisters the push token, so a signed-out device keeps receiving the previous user's notifications

- **alan:** authsec · **iş yükü:** small

**Kanıt:** apps/consumer/src/lib/auth-context.tsx:141-150 — `logout()` calls `client.auth.logout()`, `clearStoredRefreshToken()` and `signOutLocally()`, and nothing else; no push-token call. `client.account.pushTokens.remove` exists (packages/api-client/src/domains/account.ts:26-29, DELETE /api/me/push-tokens/{token}) and a repo-wide grep for `pushTokens` across apps/, landing/ and e2e/ returns exactly one hit — the register call at apps/consumer/src/lib/push.ts:46. The backend row therefore stays bound to the old userId until someone else registers the same Expo token (PushTokensService.register upserts on expoPushToken and reassigns userId, push-tokens.service.ts:25-30), and `remove` was written user-scoped precisely for this (:36-43). The payload claim holds: RESERVATION_CONFIRMED / RESERVATION_CANCELLED_REFUND / PICKUP_REMINDER are `transactional: true` (notification-policy.table.ts:44-46) so they bypass the preference and quiet-hours gates (notification-policy.service.ts:95), and reservation-confirmed.handler.ts puts the bearer code in the body verbatim (`Teslim alma kodun: ${payload.code}`).

**Düzeltme taslağı:** In apps/consumer/src/lib/push.ts add `export async function unregisterPushToken(): Promise<void>` that resolves the Expo token the same way registerPushTokenIfPermitted does (getExpoPushTokenAsync with the same projectId resolution — or cache the last-registered token in a module variable at push.ts:46 and read it here) and calls `await client.account.pushTokens.remove(token)`, never throwing. Call it from auth-context.tsx's `logout()` BEFORE `client.auth.logout()` (the access token must still be valid for the DELETE), inside the same best-effort try/catch.

### I7 — POST /me/location has no caller on any surface — the OFFER_NEARBY push audience is permanently empty

- **alan:** deadcode · **iş yükü:** small

**Kanıt:** `client.account.updateLocation` is still defined only, never called: `grep -rn "updateLocation|me/location" apps/*/src packages/*/src landing e2e backend/src` returns exactly two non-generated hits — packages/api-client/src/domains/account.ts:17-19 (the definition) and packages/api-client/src/generated/openapi-types.ts:744. The only writer of the columns is backend/src/modules/notifications/user-location.service.ts:22 (`data: { lastLat, lastLng, lastLocationAt }`), reachable only via POST /me/location; every other `lastLat` hit in backend/ is a spec fixture or a migration (offer-published-fanout.realdb.spec.ts, migration.sql). backend/src/modules/outbox/handlers/offer-published.handler.ts:159-160 still filters `AND u."lastLat" IS NOT NULL AND u."lastLng" IS NOT NULL` before ST_DWithin, so queryNearbyUserIds returns [] for every publish in production. The user-facing controls are still shipped: apps/consumer/src/app/notification-preferences.tsx:85-86,143-145 (nearbyEnabled toggle + nearbyRadiusM). apps/consumer/src/hooks/use-effective-location.ts:14-31 obtains GPS and keeps it purely client-side; the only `client.account.*` calls in apps/consumer are pushTokens.register (lib/push.ts:46) and notificationPreferences get/update (hooks/use-notification-preferences.ts:9,24).

**Düzeltme taslağı:** In apps/consumer/src/hooks/use-effective-location.ts, after `setGpsLocation(coords)` (line 28), fire-and-forget `client.account.updateLocation({ lat: coords.lat, lng: coords.lng }).catch(() => undefined)` — guarded on signed-in status and throttled (e.g. skip if the last post was < 15 min ago, stored in AsyncStorage) so Discover+Search mounting together don't double-post. Alternative if the write path is deliberately deferred: hide the nearbyEnabled/nearbyRadiusM controls in notification-preferences.tsx until it exists.

### I8 — Consumers can file complaints but never read them — GET /complaints/mine and GET /complaints/{id} have no caller

- **alan:** deadcode · **iş yükü:** medium

**Kanıt:** `grep -rn "complaints\.listMine|complaints\.get(" apps/*/src packages/api-client/src landing e2e` returns exactly one hit — apps/admin-web/src/features/complaints/useComplaints.ts:103, which is `client.admin.complaints.get`, a different (admin) domain. The consumer-scoped methods packages/api-client/src/domains/complaints.ts:12 (`listMine`) and :16 (`get`) have zero callers. apps/consumer still only writes: hooks/use-complaints.ts:18 calls `client.complaints.create` and nothing else; the expo-router tree (apps/consumer/src/app/) has `complaint/new` only — no list or thread screen — and _layout.tsx:82 registers just `complaint/new`, linked from (tabs)/profile.tsx:92-93. Backend backend/src/modules/complaints/complaints.controller.ts:29 (@Actors("CONSUMER")), :49 (@Get("mine")), :61 (@Get(":id")) are live and unreachable from any surface. The merchant side was fixed separately (commits bbf7cd6/07cfbb2 added complaints.getAssigned/listAssigned), which does not touch the consumer read path — so the admin's resolution message posted via addMessage is still visible to admins only.

**Düzeltme taslağı:** Add two consumer screens: apps/consumer/src/app/complaints/index.tsx (useQuery → `client.complaints.listMine({page:1,pageSize:50})`, rows showing category/status/SLA deadline) and apps/consumer/src/app/complaints/[id].tsx (`client.complaints.get(id)` for the thread + a reply box calling `client.complaints.addMessage(id, {body})`). Register both in apps/consumer/src/app/_layout.tsx next to the existing `complaint/new` entry, and add a "Şikayetlerim" row in (tabs)/profile.tsx beside the existing complaint CTA (profile.tsx:92).

### I9 — The redeem screen never shows the pickup window it is being judged against, and every rejection reason collapses to one vague Turkish sentence

- **alan:** consumer · **iş yükü:** medium

**Kanıt:** apps/consumer/src/app/redeem/[id].tsx:75 — `isRedeemable = data.reservation.status === "CONFIRMED"` is still the ONLY gate; the ready-state render (lines 120-165) shows storeName, bagTitle, LiveClock, code, qty and the SwipeToConfirm, with no pickup time anywhere (`data.pickupStartAt`/`data.pickupEndAt` are available on the hook's return but never read on this screen — grep of the file shows only `formatClockTime` used, at line 99, for the success timestamp). Backend unchanged: backend/src/modules/reservations/reservations.service.ts:938-946 still throws one `notRedeemableError()` for `status !== CONFIRMED || now < pickupStartAt || now > pickupEndAt`, and :194-199 defines that as the single `RESERVATION_NOT_REDEEMABLE` code. Consumer maps it to one line (apps/consumer/src/i18n/tr.json:313 "Bu sipariş şu anda teslim alınamıyor.") rendered as small text at redeem/[id].tsx:162-164 via getErrorMessage (apps/consumer/src/lib/errors.ts:14-18); merchant-web's copy is equally reason-free (apps/merchant-web/src/i18n/locales/tr/errors.json:36). The reachable-but-unreachable explanation is confirmed: the only screen with window copy is the notRedeemable EmptyState at lines 77-89 (tr.json redeem.notRedeemableBody "Teslim alma penceresi henüz başlamamış ya da geçmiş olabilir"), which cannot render while status is CONFIRMED. The compounding claim also still holds: backend/src/modules/reservations/dto/reservation-response.dto.ts:13-35 (ReservationDto) has no pickupStartAt/pickupEndAt, so apps/consumer/src/hooks/use-order-details.ts:100-116 falls back to `derivePickupStartAt(cancelDeadlineAt)` with `pickupEndAt: null` on a device with no purchase snapshot and no same-day live offer. git log shows no commit touched apps/consumer/src/app/redeem/ in the blocking round (last touch: edca101, doc comments + money format).

**Düzeltme taslağı:** Two parts, smallest first. (1) Consumer-only, removes the bite at the counter: in redeem/[id].tsx compute `start = new Date(data.pickupStartAt)` and `end = data.pickupEndAt`; render a window line under LiveClock (`formatPickupWindow(data.pickupStartAt, data.pickupEndAt)` when end is known, else `formatClockTime(data.pickupStartAt)`); when `Date.now() < start`, replace SwipeToConfirm with a disabled control plus a new pre-emptive key (`redeem.notStartedYet`: "Teslim alma {{time}}'da başlıyor") in tr.json/en.json instead of letting the swipe fail; when `end` is known and passed, fall through to the existing notRedeemable EmptyState. (2) So the too-late branch works on a reinstalled device and the error text carries a reason: add `pickupStartAt`/`pickupEndAt` to ReservationDto (they are already on the loaded `reservation.offer` in redeem()/list paths), and split notRedeemableError into RESERVATION_PICKUP_NOT_STARTED / RESERVATION_PICKUP_WINDOW_PASSED / RESERVATION_NOT_REDEEMABLE with matching keys in apps/consumer/src/i18n/{tr,en}.json and apps/merchant-web/src/i18n/locales/*/errors.json. Part (1) alone is `small`; with the DTO+code split it is `medium`.

### I10 — SLA countdowns are frozen at fetch time on the two queues where they are acted on

- **alan:** adminweb · **iş yükü:** trivial

**Kanıt:** apps/admin-web/src/App.tsx:19-26 (QueryClient defaults: retry:1, refetchOnWindowFocus:false — no refetchInterval, no staleTime); apps/admin-web/src/features/complaints/useComplaints.ts:36-92 (useQuery with only queryKey+queryFn); apps/admin-web/src/features/moderation/useReports.ts:11-29 (same); contrast apps/admin-web/src/features/dashboard/useDashboardData.ts:10,18,45,62,94 (refetchInterval: 60_000 on all four dashboard queries — grep for refetchInterval across apps/admin-web/src returns ONLY those five lines). Consumers of the frozen number: apps/admin-web/src/features/complaints/ComplaintsListPage.tsx:176-179 and apps/admin-web/src/features/moderation/ReportsPage.tsx:189-191 pass item.slaCountdownMs / report.takedownCountdownMs straight to DeadlineBadge; apps/admin-web/src/lib/countdown.ts:1-10 confirms the client never recomputes from its own clock; the AT_RISK synthetic filter re-derives from the same frozen field at useComplaints.ts:71-79. git log on both hook files shows no commit in the fix round changed this.

**Düzeltme taslağı:** Add `refetchInterval: 60_000` (matching useDashboardData.ts's REFRESH_INTERVAL_MS — export it or lift it to a shared lib/query-config.ts constant) to the useQuery options in useComplaintsList (useComplaints.ts:37) and useReportsList (useReports.ts:19). That alone re-syncs both badges and the AT_RISK filter to the server-computed deadline once a minute. Optional hardening: also set `refetchOnWindowFocus: true` on just those two keys so an alt-tab back is instantly honest. Scope note: the 48h takedown queue is the one that actually bites (3h critical window); the 15-day complaint SLA drifts too but far less consequentially. Blast radius is limited to a continuously-open tab — React Query's default refetchOnMount already refreshes on navigation — but that is the ops-console usage pattern, and the drift is always in the unsafe direction (shows more time than remains, and a report that crosses into critical never re-styles or enters the filter).

### I11 — The STT attestation checkbox does not say what the STT undertaking is, and the recorded contract version can't be resolved to a published document

- **alan:** crosssurface · **iş yükü:** small

**Kanıt:** Every element of the finding is unchanged. (1) apps/merchant-web/src/i18n/locales/tr/onboarding.json:32 still reads `"stt": "Satış Sözleşmesi ve Teslim Taahhüdü'nü okudum ve kabul ediyorum."` — a document that still does not exist in the repo: `grep -rni "Teslim Taahhüdü"` over all non-node_modules ts/tsx/json returns only this one string, and landing/content/legal/ contains only aracilik-sozlesmesi, mesafeli-satis-sozlesmesi, on-bilgilendirme-formu, kvkk-aydinlatma-metni, cerez-politikasi. (2) The real undertaking is still defined only in landing/content/legal/aracilik-sozlesmesi.ts:110 ('İşletme, son tüketim tarihi geçmiş hiçbir ürünü Sürpriz Paket içeriğine dahil etmeyeceğini kabul ve taahhüt eder ("STT Taahhüdü")'), whose text the checkbox never shows. (3) Neither checkbox links anywhere — apps/merchant-web/src/onboarding/OnboardingPage.tsx:138-149 passes plain `t(...)` strings with no `href`, even though `Checkbox` already accepts `label: ReactNode` (shared/ui/Checkbox.tsx:8). (4) `CONTRACT_VERSION = "2026-08"` is still hand-typed at OnboardingPage.tsx:17 and shipped as `intermediationContractVersion` (:38), while the published document stamps `versionLabel: { tr: "v0.1 — 15 Ağustos 2026" }` (aracilik-sozlesmesi.ts:75-78) — two unrelated identifiers.

The enforcement chain is intact, so the evidentiary hole is real: backend/src/modules/merchants/merchants.service.ts:184-186 rejects submit without `sttAttestationAccepted`, and :204-208 stamps `sttAttestationAcceptedAt` + stores `dto.intermediationContractVersion` verbatim into a free-form `@IsString()` field (dto/merchant-submit.dto.ts:36). That timestamp is the platform's ONLY proof of the STT undertaking, and it is bound to a checkbox whose visible text never mentions son tüketim tarihi.

Kept at important, and arguably stronger now: commit 2e06e1d built exactly the missing pattern for the consumer side — apps/consumer/src/content/legal/ (a mirrored copy of the five documents), a `legal/[doc].tsx` viewer screen, and a purchase-gate consent label that names the real documents and is regression-tested (apps/consumer/src/__tests__/purchase-screen.test.tsx:163-173 asserts 'links to the real … documents, not a placeholder'). merchant-web is now the one surface whose consent still points at a document that does not exist.

**Düzeltme taslağı:** Three edits, tr is the only locale merchant-web ships (apps/merchant-web/src/i18n/locales/ contains just `tr`, so no parity mirror needed). (a) Rewrite onboarding.json:32 in the agreement's own words, e.g. `"stt": "Sürpriz Paket içeriğine son tüketim tarihi geçmiş hiçbir ürün dahil etmeyeceğimi kabul ve taahhüt ederim (STT Taahhüdü — Aracılık Sözleşmesi Madde 3)."` (b) In OnboardingPage.tsx:138-149, pass a ReactNode label with an `<a target="_blank">` to the published document — `${import.meta.env.VITE_LANDING_URL}/tr/yasal/aracilik-sozlesmesi` (the route exists: landing/app/[locale]/yasal/[slug]/page.tsx), on both checkboxes. (c) Replace the hand-typed constant at :17 with the document's own label — `const CONTRACT_VERSION = "v0.1 — 15 Ağustos 2026"` with a comment naming aracilik-sozlesmesi.ts:75-78 as its source, or better, mirror the consumer's approach and import `versionLabel.tr` from a copied content module so a version bump can't drift. Effort is small for (a)+(c) plus a link; it becomes medium only if you also mirror the full legal content into merchant-web the way apps/consumer/src/content/legal/ does.

### I12 — Automated rollback reverts the api image past an already-applied schema change and declares success on a DB-free liveness probe

- **alan:** ops · **iş yükü:** small

**Kanıt:** .github/workflows/release-deploy.yml:743 rollback step; :767 `up -d --force-recreate api` (api only — merchant-web/admin-web/landing stay on the new tag); :770 success gate is `curl http://127.0.0.1:4750/api/health` and prints "Rollback succeeded — api is healthy again on ${prev_tag}". backend/src/modules/health/health.controller.ts:21-29 returns a static {status,service,uptimeSec} — grep for `ready|readiness` under backend/src/modules/health/ returns nothing, so there is no DB-touching probe anywhere. Migrations still run first (:679 `run --rm --workdir /app/backend api npx prisma migrate deploy`) and the swap is at :690, so the ordering the finding describes is intact. The concrete destructive migration is still on the branch: backend/prisma/migrations/20260815200000_social_trust_and_moderation/migration.sql:95-97 `ALTER TABLE "complaint_tickets" DROP COLUMN "category"` then re-adds it as the "ComplaintCategory" enum — the previous image's generated client selects it as TEXT and every complaints/SLA query (incl. the 15-day ETAHS cron) errors, while /api/health answers in ms. Partial mitigation that did NOT exist per the finding text but does now bound the blast radius: the second rollback step (:774, `if: failure() && steps.migrate.conclusion != 'skipped'`) always fires once migrate ran and prints the pre-deploy backup restore command, and the whole job is red regardless — so the operator is misled by one step's message, not by a green pipeline. The api-only scope is acknowledged in the workflow header at :89-94 as a KNOWN GAP.

**Düzeltme taslağı:** Two edits, no new infrastructure. (1) Add `GET /api/ready` to health.controller.ts — `@Public()`, injects PrismaService and does `await prisma.$queryRaw`SELECT 1`` (plus a Redis PING if the client is already provided), returning 503 on failure — kept distinct from /api/health so the compose HEALTHCHECK at backend/Dockerfile:98 keeps using the cheap liveness route. (2) In release-deploy.yml:770 change the rollback's success loop to curl `/api/ready`, and at :767 recreate all four services (`up -d --force-recreate api merchant-web admin-web landing`) — IMAGE_TAG has already been rewritten at :764, so the frontends are one word away from being rolled back too. Optionally also point the post-deploy probe at :701 to /api/ready so a schema-incompatible forward deploy fails the same way.

### I13 — M1 — A complaint's 15-day ETAHS clock starts with no notification to the merchant who has to answer it

- **alan:** minors · **iş yükü:** medium

**Kanıt:** backend/src/modules/complaints/complaints.service.ts:108-130 — ComplaintsService.create still writes the ticket with no outbox.publish; `grep -rn 'outbox\|publish' backend/src/modules/complaints/*.ts` returns zero hits, and `grep -n 'complaint' backend/src/modules/outbox/event-types.ts` returns nothing, so OUTBOX_EVENT_TYPES still has no complaint event. complaint-sla-cron.service.ts:145-157 escalateBreached's WHERE is still `status IN ('OPEN','MERCHANT_RESPONDED')`, so an ESCALATED ticket is never touched again.

**Düzeltme taslağı:** Add `complaint.created.v1` to OUTBOX_EVENT_TYPES and publish it inside the same transaction as the ticket insert in ComplaintsService.create, with a handler that reuses the existing merchant-email pattern (the one behind merchant.approved / settlement.sent). Second, separate step: give escalateBreached a companion sweep (or an admin dashboard tile) over ESCALATED-and-unresolved tickets so the resolution obligation has an ongoing clock.

### I14 — M4 — Full IBAN/taxId is exposed on two admin surfaces without the audit trail the third one establishes as the invariant

- **alan:** minors · **iş yükü:** small

**Kanıt:** settlements.service.ts:66-77 — adminGet still selects `merchant: { select: { tradeName, legalName, iban } }` with no AuditLog write, and it is the response body of the detail read and of approve/hold/retry. admin-exports.service.ts:130-163 — streamMerchantsCsv still streams legalName, taxId, iban for every merchant, and `grep -rn auditLog backend/src/modules/admin/*.ts` returns nothing, so neither the service nor admin-exports.controller.ts:53 writes an audit row. merchants.service.ts's adminGetDetail remains the only place that honours the stated 'ONLY with an audit trail' invariant. Raised to important: a bulk PII/bank-detail exfiltration path with zero forensic trace is a KVKK-relevant gap, not a cosmetic one.

**Düzeltme taslağı:** Two-line fix on the export: in AdminExportsController's merchants.csv handler, write an AuditLog row (`action: 'merchant.kyc.exported'`, actorId = the admin, entity 'Merchant', metadata = the requested range) before streaming. Separately, drop `iban`/`legalName` from SettlementsService.adminGet's merchant select — the settlement detail screen renders neither.

---

## MINOR (29)

### M1 — Refresh cookie is not Secure and CORS falls back to the localhost dev allowlist on staging, so the whole session scheme degrades

- **alan:** authsec · **iş yükü:** small

**Kanıt:** backend/src/modules/auth/refresh-cookie-transport.util.ts:114 still reads `secure: process.env.NODE_ENV === "production"`; backend/src/main.ts:49 still returns DEV_DEFAULT_CORS_ORIGINS (the four http://localhost:* origins, with credentials:true at main.ts:92-97) for any `NODE_ENV !== "production"`. `staging` is a validated env (backend/src/config/env.validation.ts:77-83 VALID_NODE_ENVS) and a documented deployment target (ops/docker-compose.staging.yml, api published on 0.0.0.0:4760, panels on 4766/4767/4768; docs/operations.md:12,18,41). env.validation.ts:156 (`const isProduction = nodeEnv === "production"`) confirms every security gate is is-production, not is-deployed; ops/docker-compose.staging.yml lists no CORS_ALLOWED_ORIGINS in its required-variable header (only prod's does, docker-compose.prod.yml:35). Nothing in the 12-commit round touched either line (6b35064 rewrote cookie naming/paths only).

**Düzeltme taslağı:** Export `isLocalEnv(nodeEnv)`/`isDeployedEnv(nodeEnv)` from backend/src/config/env.validation.ts and use it in both places: `secure: isDeployedEnv(process.env.NODE_ENV)` in setRefreshCookie, and `if (isLocalEnv(process.env.NODE_ENV)) return DEV_DEFAULT_CORS_ORIGINS;` in resolveCorsOrigins so a deployed env with no CORS_ALLOWED_ORIGINS gets no CORS at all. Caveat that shapes the fix: ops/docker-compose.staging.yml terminates no TLS itself (unlike prod's documented nginx/tunnel hop, docker-compose.prod.yml:33), so hard-coding Secure for staging would break a plain-HTTP staging — prefer driving it from an explicit `REFRESH_COOKIE_SECURE` (default = isDeployedEnv) and add both it and CORS_ALLOWED_ORIGINS to the staging compose's required-variable header. Also update the two specs that assert today's behaviour (auth.controller.transport.spec.ts:230, merchants.controller.transport.spec.ts:99).

### M2 — SameSite=strict refresh cookie silently assumes a same-site topology the CORS design does not guarantee

- **alan:** authsec · **iş yükü:** small

**Kanıt:** backend/src/modules/auth/refresh-cookie-transport.util.ts:115 is still a hardcoded `sameSite: "strict" as const` with no env override, while main.ts:92-97 enables credentialed cross-origin CORS. A repo-wide grep for samesite/same-site/registrable finds the attribute only in the util and its two transport specs — docs/launch-checklist.md:44 still says only "confirm the real origin topology … refresh cookies actually Secure", and docs/frontend-contract.md:184 and :218 describe per-actor cookies and CORS without ever stating the same-registrable-domain requirement. The origin topology is genuinely unpinned: every app's base URL is a build-time env var (apps/*/.env.example:3, landing/.env.example:4, docs/launch-checklist.md:55's PUBLIC_API_BASE_URL).

**Düzeltme taslağı:** Two lines of work, both cheap: (1) state the constraint at the cookie definition and as its own launch-checklist item — 'every browser surface MUST be served from the same registrable domain as the API, or the refresh cookie is never sent; verify the cookie is present on the SECOND request from the real panel origin'; (2) make it configurable — `REFRESH_COOKIE_SAMESITE` (default "strict", enum-validated in env.validation.ts alongside the other provider enums) and force `secure: true` whenever it resolves to "none".

### M3 — admin-web also drops the access token from login, making the admin session wholly dependent on the refresh cookie

- **alan:** authsec · **iş yükü:** trivial

**Kanıt:** apps/admin-web/src/auth/AuthContext.tsx:133-139 — `login()` awaits `client.auth.adminLogin(...)` and uses only `result.user`; `setStoredAccessToken` appears in the app only at :75 and :151, both `null` clears (grep over apps/admin-web/src confirms). packages/api-client/src/engine.ts fires `options.onTokensIssued?.(tokens)` at exactly one place — engine.ts:201 inside `performRefresh()` — so adminLogin (domains/auth.ts:37-41, a plain `engine.request`) never populates apps/admin-web/src/api/client.ts:59-61. Recovery is via the 401 branch at engine.ts:238-243. merchant-web does it correctly at apps/merchant-web/src/auth/AuthContext.tsx:99 (`setAccessToken(result.accessToken)`), and apps/consumer does it at src/lib/auth-context.tsx:125 — admin-web is the only surface left.

**Düzeltme taslağı:** One line in apps/admin-web/src/auth/AuthContext.tsx's `login()`, immediately after `adminLogin` resolves: `setStoredAccessToken(result.accessToken);` (before `setStatus("authenticated")`), mirroring apps/merchant-web/src/auth/AuthContext.tsx:99. No tests reference it (apps/admin-web/src/auth/ has no spec).

### M4 — The share-link → app hand-off deep-links to a route apps/consumer does not have, with a package name that can never match

- **alan:** deadcode · **iş yükü:** trivial

**Kanıt:** Neither half changed. (a) apps/consumer/src/app/ still contains only (auth), (tabs), cancel, complaint, legal, notification-preferences, offer, order, payment, purchase, rate, redeem, report, store, index.tsx, _layout.tsx — there is no `o/` directory and no `+not-found.tsx`; _layout.tsx:67-85 registers offer/[id], store/[id], order/[id] etc. but nothing at /o/…, and there is no expo-router `linking` config anywhere in apps/consumer that could remap it. (b) landing/lib/site-config.ts:51 still reads `androidPackageName: "app.kurtar.consumer.PLACEHOLDER"` while apps/consumer/app.json:22 declares `"package": "app.kurtar.consumer"`; landing/components/OfferAppOpener.tsx:32 interpolates that package into the intent:// URL and :34 emits `kurtar://o/<id>` on iOS. Severity downgraded from important to minor: nothing yet generates these links — `grep -rn "/o/"` across apps and landing finds only the bridge page itself (landing/app/[locale]/o/[id]/page.tsx:28), robots.ts:13 and sitemap.ts:8, and apps/consumer has no Share affordance at all — and the app is unpublished, so the path only bites once share links and a store listing exist. The defect is real and will bite at launch.

**Düzeltme taslağı:** Add apps/consumer/src/app/o/[id].tsx as a two-line redirect (`const { id } = useLocalSearchParams(); return <Redirect href={`/offer/${id}`} />;`) plus a `<Stack.Screen name="o/[id]" />` entry in _layout.tsx, and an apps/consumer/src/app/+not-found.tsx branded fallback. In landing/lib/site-config.ts:51 set `androidPackageName: "app.kurtar.consumer"` (the real declared package), leaving only iosAppStoreId and the Play Store listing URL marked as pre-launch placeholders.

### M5 — Pickup countdown prints mm:ss for a pickup hours away — "Teslim alma: 18:30 · 420:00"

- **alan:** consumer · **iş yükü:** trivial

**Kanıt:** apps/consumer/src/lib/format.ts:59-65 is unchanged: `formatCountdown` still computes `minutes = Math.floor(total / 60)` with no hour rollover, padded to 2 digits. Its only caller is apps/consumer/src/components/PickupCountdown.tsx:29 (`{remaining > 0 ? ` · ${formatCountdown(remaining)}` : ""}`), rendered for every CONFIRMED reservation on the Orders list (apps/consumer/src/components/OrderRow.tsx:71-73, off a pickupStartAt derived at OrderRow.tsx:47) and on the order detail screen (apps/consumer/src/app/order/[id].tsx:67-70). The component's own doc comment at PickupCountdown.tsx:11-13 still advertises "Teslim alma: 18:30 (2s 14dk sonra)" — implementation and documented design still disagree. Test coverage still stops at 65s: apps/consumer/src/__tests__/format.test.ts:34-39 (65_000, 3_000, -5_000, 0). A 11:30 reservation for an 18:30 pickup therefore renders "Teslim alma: 18:30 · 420:00". Downgraded from important to minor: it is presentation-only, the correct absolute time ("18:30") is printed immediately to its left, and no decision or money depends on it — but it is 100% reproducible and is the default state of the app's primary list for essentially every active order.

**Düzeltme taslağı:** Add to apps/consumer/src/lib/format.ts a range-aware `formatRemaining(msRemaining)`: <1h -> `${minutes} dk`; <24h -> `${hours} sa ${minutes} dk` (drop the ` 0 dk` tail); >=24h -> `${days} gün ${hours} sa`. Use it at PickupCountdown.tsx:29 in place of `formatCountdown` (keep `formatCountdown` only if a genuinely short-timer surface appears; it currently has no other caller, so it can simply be replaced). Extend format.test.ts with a multi-hour case (25_200_000 -> "7 sa") and a sub-hour case.

### M6 — [ADDITIONAL — same class as I15] OFFER_UNAVAILABLE is a deliberately collapsed code, but the consumer renders it as a confident and often false reason ("Bu paket az önce tükendi — başka biri senden önce davrandı")

- **alan:** consumer · **iş yükü:** trivial

**Kanıt:** backend/src/modules/reservations/offer-stock.service.ts:27-35 documents the collapse explicitly and on purpose: "insufficient room, offer not published, a nonexistent id, or a non-APPROVED merchant — all collapse to `false`; the caller is responsible for turning that into the uniform OFFER_UNAVAILABLE error, so a probing caller can never distinguish 'sold out' from 'this merchant is suspended'". reservations.service.ts:337-340 turns every one of those into `offerUnavailableError()` (defined :162-168). The consumer then asserts a single specific cause: apps/consumer/src/app/purchase/[offerId].tsx:68-88 catches it via `isOfferUnavailableError` and renders a full-screen EmptyState with tr.json purchase.unavailableTitle "Bu paket az önce tükendi" and unavailableBody "Çok popüler bir paketti — başka biri senden önce davrandı." Reachable path: a consumer opens a stale discovery card for an offer the merchant has since CLOSED/CANCELLED (or whose merchant was suspended — merchants suspend -> ReservationsService.cancelAllForOffer, whose sweep is best-effort so PUBLISHED leftovers exist by design) and is told a factually wrong story about why. Minor: the user's only available action (find another bag) is unchanged, and the server-side opacity is a security property that must be preserved.

**Düzeltme taslağı:** Copy-only, in apps/consumer/src/i18n/tr.json + en.json: make purchase.unavailableTitle/Body state the outcome instead of inventing a cause — e.g. title "Bu paket artık alınamıyor", body "Tükenmiş ya da mağaza tarafından kaldırılmış olabilir. Yakınındaki diğer paketlere göz atabilirsin." Keep the same EmptyState and CTA; change nothing on the backend (the collapse is intentional).

### M7 — [ADDITIONAL — same class as I15] The payment screen tells a consumer whose offer was pulled that their payment failed and invites a retry that cannot succeed

- **alan:** consumer · **iş yükü:** trivial

**Kanıt:** apps/consumer/src/app/payment/[id].tsx:16-20 defines TERMINAL_FAILURE_STATUSES = {CANCELLED_BY_USER, CANCELLED_BY_MERCHANT, EXPIRED} and lines 62, 90-102 render one branch for all of them: tr.json payment.failedTitle "Ödeme tamamlanamadı" + failedBody "Ödemen onaylanmadı ya da iptal edildi. Tekrar deneyebilirsin." But EXPIRED is not only the payment-timeout status: backend/src/modules/reservations/reservations.service.ts:703 and :731-734 (cancelAllForOffer) flip a PENDING_PAYMENT reservation to EXPIRED when the MERCHANT cancels the offer or an admin suspends the merchant. So a consumer sitting on the provider WebView while the merchant pulls the offer is told their payment was not approved (false) and invited to retry (impossible — the offer is CANCELLED, so a retry hits OFFER_UNAVAILABLE). Minor: no money is at stake on this branch (the same transaction sets the Payment to FAILED at :726), and the frequency is low — it needs a cancel inside the payment window.

**Düzeltme taslağı:** Copy-only: soften payment.failedBody in apps/consumer/src/i18n/{tr,en}.json to cover both causes without asserting one — e.g. "Rezervasyonun tamamlanmadı: ödeme onaylanmadı ya da paket artık mevcut değil. Diğer paketlere göz atabilirsin." and keep the existing back-to-discover button as the primary action rather than implying a retry. (A precise message would need a cancellation reason on the reservation read, which is out of proportion to the impact.)

### M8 — The breached SLA badge is the only urgency state that renders no border — an undefined token silently drops one of its three non-colour signals

- **alan:** adminweb · **iş yükü:** trivial

**Kanıt:** apps/admin-web/src/components/DeadlineBadge.module.css:51 still reads `border: 2px solid var(--color-danger-900);`. packages/ui-tokens/src/colors.ts:76-80 defines semantic.danger with only steps 50/500/700 (no 900 — the 900 hits at colors.ts:39/53/105 are primary, secondary and neutral ramps). apps/admin-web/src/styles/tokens.ts:30-34 injects exactly the steps each ramp actually has, so --color-danger-900 is never set; `grep -rn color-danger apps/admin-web/src/styles/global.css` returns nothing, and a repo-wide grep for `danger-900` matches only DeadlineBadge.module.css and the stale built bundle apps/admin-web/dist/assets/index-CmYYButX.css. Unresolved var() in a shorthand => invalid at computed-value time => border-style falls back to its initial `none`, while .safe:32, .warning:38 and .critical:44 all reference tokens that do exist. Downgraded from the finding's framing because two of the three documented signals (DeadlineBadge.tsx:25-35) survive intact: the `✖` glyph (DeadlineBadge.tsx:22) and the always-rendered bold urgency word 'SÜRESİ DOLDU' (DeadlineBadge.tsx:64,77), and .breached is still a solid --color-danger-700 fill with --color-neutral-0 text, visually distinct from .critical's light-background outlined pill. No operator misses a deadline over this.

**Düzeltme taslağı:** Change DeadlineBadge.module.css:51 to `border: 2px solid var(--color-danger-700);` — or, if a darker edge is wanted against the danger-700 fill, add a `900` step to `semantic.danger` in packages/ui-tokens/src/colors.ts (the injector at styles/tokens.ts:30-34 iterates the ramp, so no bridge change is needed in admin-web; merchant-web's own bridge would need the same iteration check). Either way add the cross-cutting guard the finding asks for: a vitest in apps/admin-web (and merchant-web) that regexes every `var(--…)` out of src/**/*.module.css and asserts the name is in the set injectDesignTokens() produces — the same enforcement landing/test/palette-parity.test.ts already has. That guard is the part worth the effort; the one-line token swap is seconds.

### M9 — Three surfaces, three timezone policies: merchant-web pins Europe/Istanbul, admin-web and consumer use the viewer's clock

- **alan:** crosssurface · **iş yükü:** trivial

**Kanıt:** Unchanged by the 12-commit round (last touch of apps/consumer/src/lib/format.ts is edca101, which only fixed money formatting). apps/admin-web/src/lib/date.ts:1-13 — both `Intl.DateTimeFormat("tr-TR", {...})` instances still omit `timeZone`. apps/consumer/src/lib/format.ts:31-37 (`formatClockTime`), :45-51 (`formatClockWithSeconds`), :54-57 (`formatShortDate`) all call `toLocale*("tr-TR", …)` with no `timeZone`. Contrast apps/merchant-web/src/shared/format.ts:14-36, which pins `timeZone: "Europe/Istanbul"` on all four formatters plus `istanbulDateKey` at :54-67. `grep -rn "Europe/Istanbul" apps/consumer/src apps/admin-web/src` returns nothing.

CAN IT MISLEAD? Yes — it is not cosmetic on the consumer side. `formatClockTime` feeds (a) the pickup window on every offer card, offer detail and store page (apps/consumer/src/components/OfferCard.tsx:57, app/offer/[id].tsx:112, app/store/[id].tsx:115 via `formatPickupWindow`), (b) the free-cancellation deadline text (apps/consumer/src/app/cancel/[id].tsx:67, `cancel.deadlineNote`), and (c) the active-order row (components/PickupCountdown.tsx:28). Any device whose clock is not on Europe/Istanbul renders all of these shifted by the full offset difference — a paid 18:30–20:00 pickup reads 17:30–19:00 on a CET phone, and the cancel deadline reads an hour earlier/later than the one the server enforces. A missed pickup forfeits a paid bag, so the harm is real money, not display polish. Its own doc comment at format.ts:31 asserts the opposite of what the code does — '"18:30" from an ISO instant, Istanbul-local via the runtime's tr-TR formatter' — the tr-TR *locale* does not pin a timezone, so this is a bug against a stated invariant, not a design choice.

Severity downgraded to minor because the trigger is narrow: the app is Turkey-only and device timezone is auto-set from the network for essentially the whole user base, so the affected population is travellers/expats with a manually-pinned foreign clock (plus emulators, which is why this survived testing).

The admin-web half is weaker than the finding claims. Batch periods are not arbitrary instants: `periodStart = offerDateToDbDate(dayKey)` is exactly UTC midnight (settlement-batch-builder.service.ts:439, istanbul-date.util.ts:22-24) and `periodEnd = periodStart + 24h` (:440). At UTC midnight, every zone from UTC through UTC+3 renders the SAME calendar day, so an operator anywhere in Turkey or Europe sees the same date merchant-web shows; the day-shift the finding describes only appears on a laptop in a negative-offset zone (Americas). The example in the finding (periodEnd at 21:30Z) does not correspond to data this system produces. Same for `dueAt` and PlatformPricing `effectiveFrom` (admin-web SettlementDetailPage.tsx:115-116, SettlementsListPage.tsx:121-122, DashboardPage.tsx:181,207, PricingPage.tsx:117,205).

**Düzeltme taslağı:** Two-line change, no consolidation needed for correctness: add `timeZone: "Europe/Istanbul"` to both `Intl.DateTimeFormat` options objects in apps/admin-web/src/lib/date.ts:1-13, and add `timeZone: "Europe/Istanbul"` to the three `toLocaleTimeString`/`toLocaleDateString` option objects in apps/consumer/src/lib/format.ts:33-36, :46-50, :56. The doc comment at format.ts:31 then becomes true. If you want the tidier version afterwards, move merchant-web's istanbul-pinned formatters into @kurtar/ui-tokens next to `formatMoneyCents` — but that is cleanup, not the fix.

### M10 — landing still double-casts a client response through a hand-written shape, citing a bug that is fixed

- **alan:** crosssurface · **iş yükü:** trivial

**Kanıt:** Both the cast and the stale 24-line justification are still present verbatim: landing/lib/impact.ts:60-82 (the comment claiming 'EVERY domain method's compiled dist/*.d.ts resolves to Promise<never>') and :83 (`const totals = (await client.impact.getPublic()) as unknown as {...}`). git log on landing/lib/impact.ts shows no commit in the blocking round touched it.

The comment's premise is false against the current build. packages/api-client/dist/domains/impact.d.ts declares `getPublic: () => Promise<{ mealsSaved: number; co2eGrams: number; moneySavedCents: number; count: number; generatedAt: string; }>` — a fully resolved type, not `never`. `SuccessBody` was fixed (packages/api-client/src/core-types.ts:88-103, the `` `${K}` `` template-literal coercion) and its own doc comment says it is regression-gated against the BUILT declaration output via test-types/build-output.types.ts / `npm run typecheck:build`. Both other web surfaces already completed the migration and documented deleting their casts (apps/merchant-web/src/api/response-types.ts:7-14, apps/admin-web/src/api/admin-types.ts:7-22, both naming commit e5621a3); landing is the only holdout.

Severity is minor, not important: today the three field names in the cast (mealsSaved / co2eGrams / moneySavedCents) match the real DTO exactly, so nothing renders wrong right now. The defect is the lost guard — `as unknown as` silences the checker in both directions, so a future rename of any of those fields would sail past the openapi drift gate and past landing's own `tsc` while every other app's build breaks, and the home-page impact counter would render `undefined` with no thrown error for the `catch` at :94 to convert into the `unavailable` fallback.

**Düzeltme taslağı:** Delete the cast and the stale comment block: replace landing/lib/impact.ts:60-87 with `const totals = await client.impact.getPublic();`. The three field reads at :90-92 then typecheck against the generated schema (the returned type carries two extra fields, `count` and `generatedAt`, which are simply unread — no widening needed).

### M11 — Every migration is reversible in CI, but production has no way to apply a down.sql — and the runbook never says so

- **alan:** ops · **iş yükü:** trivial

**Kanıt:** Every factual claim still holds. `grep -n 'down.sql|rolled-back|revert|rollback' docs/operations.md` matches only the migration-doctor table rows at :33-34 (which discuss `migrate resolve --applied/--rolled-back` for a FAILED migration, never a down.sql revert of a successful one) — no 'Reverting a migration' section exists. scripts/db-migration-doctor.sh contains no reference to down.sql (grep). The only schema-revert guidance in the pipeline is release-deploy.yml:774-796, which offers exactly one option: `gunzip -c <backup> | docker exec -i kurtar_db_prod psql`, discarding every write since the pre-deploy backup. Meanwhile .github/workflows/quality-gates.yml:182-200 does run the real round-trip and prints "Round-trip OK — every migration's down.sql is genuinely reversible", and all 15 migration folders ship a down.sql (`ls backend/prisma/migrations/*/down.sql | wc -l` = 15, enforced at quality-gates.yml:305-323). The divergence mechanism is real: down.sql files never touch `_prisma_migrations` (stated in quality-gates.yml:171-172, which is why CI must drop the ledger itself at :197), so a hand-applied down leaves the row marked applied, `migrate deploy` skips re-applying, and db-migration-doctor.sh:153-158 cannot see it because its drift check greps `prisma migrate status` output. Downgraded from important: this is a documentation gap, not a code defect — nothing in the product does the wrong thing on its own, and the described corruption requires an operator to invent a procedure the runbook never offers.

**Düzeltme taslağı:** Add a ~10-line 'Reverting a migration' section to docs/operations.md immediately after the migration-doctor table (~line 37) saying plainly: (a) down.sql is a CI-only reversibility proof, (b) production is forward-only — `prisma migrate deploy` will never run one, and (c) if a down really must be applied by hand, the ONLY correct sequence is `cd /root/kurtar && docker compose -f docker-compose.prod.yml --env-file .env.production run --rm --workdir /app/backend api sh -c 'psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f prisma/migrations/<name>/down.sql'` followed by `... run --rm --workdir /app/backend api npx prisma migrate resolve --rolled-back <name>` so the ledger stays truthful, and that skipping the second command silently diverges the schema from the history. A scripts/db-migration-rollback.sh wrapping both atomically is the better version but is not required to close the gap.

### M12 — Both deploy commands in the operations runbook fail verbatim on the real production host

- **alan:** ops · **iş yükü:** trivial

**Kanıt:** docs/operations.md:12 still reads `docker compose -f ops/docker-compose.prod.yml pull` and :13 still reads `docker compose run --rm api npx prisma migrate deploy`. Path claim confirmed: release-deploy.yml:586-589 scps `ops/docker-compose.prod.yml` to `root@host:/root/kurtar/` (flat), and only scripts/ is nested (:595-598, with the workflow's own comment at :590-594 explaining why) — so on a pipeline-provisioned box there is no ops/ directory and step 4 fails. WORKDIR claim confirmed: backend/Dockerfile:69 sets WORKDIR /app and :87 copies prisma to /app/backend/prisma; I checked both package.json files and neither declares a `prisma` config key (root and backend/package.json both return None), so the CLI's schema lookup from /app finds nothing and step 5 exits with 'Could not find Prisma Schema'. Both the workflow (:679) and the doctor (scripts/db-migration-doctor.sh:66-77, compose_run) pass `--workdir /app/backend` for precisely this reason. The env-file point also holds: nothing between operations.md:12 and :18 mentions `--env-file .env.production` or the `.env -> .env.production` symlink the pipeline creates at release-deploy.yml:616; on a hand-provisioned host without that symlink, ops/docker-compose.prod.yml:102 `${IMAGE_TAG:?IMAGE_TAG is required}` aborts the command outright. The INERT premise still holds — release-deploy.yml:1-8 says so and `git remote -v` is empty, so a hand deploy really is the likely first one. Note step 4 is layout-dependent (correct from a repo clone, wrong on the pipeline-shipped box) while step 5's missing --workdir fails in EVERY layout. Downgraded from important: both failures are immediate and loud (missing file / 'Could not find Prisma Schema'), so nothing corrupts or deploys half-done — the cost is operator time and confusion on the first production deploy.

**Düzeltme taslağı:** Rewrite the two lines in docs/operations.md to the real on-host layout: step 4 → `cd /root/kurtar && docker compose -f docker-compose.prod.yml --env-file .env.production pull` (adding one sentence that the pipeline ships the compose file flat to /root/kurtar/ with scripts/ nested, and leaves .env symlinked to .env.production); step 5 → `docker compose -f docker-compose.prod.yml --env-file .env.production run --rm --workdir /app/backend api npx prisma migrate deploy`, with a parenthetical that --workdir is mandatory because the image's WORKDIR is /app and the schema lives at /app/backend/prisma. Same edit for the staging equivalents.

### M13 — M2 — Clawback-only sweep mints a new empty HELD batch every night for a merchant whose demand cannot be recovered

- **alan:** minors · **iş yükü:** small

**Kanıt:** settlement-batch-builder.service.ts:361-372 still calls createOrExtendBatch(merchantId, todayKey, [], new Map()); createOrExtendBatch (:433-470) find-or-creates on (merchantId, periodStart), and periodStart is derived from the dayKey, so a fresh todayKey each night = a fresh batch each night. exportableCarriedDemandCents (:87-107) returns ownFeeDeficit + carriedExternalDemand, both 0 for an empty batch, so nothing chains forward and the demand is re-discovered by findMerchantsWithPendingClawback (:379-394) the next night. IMPORTANT CHANGE SINCE THE FINDING WAS WRITTEN: the masking is gone — reservations.service.ts:578-586 documents refundRedeemed(), an admin refund of a REDEEMED reservation (landed in bbf7cd6), which is exactly the production path that creates a DONE Refund against a line in a SENT batch. This is now reachable, not hypothetical.

**Düzeltme taslağı:** In the clawback-only loop, first look for the merchant's existing open batch (`settlementBatch.findFirst({ where: { merchantId, status: { in: RECOMPUTABLE_SETTLEMENT_STATUSES } }, orderBy: { periodStart: 'desc' } })`) and recompute that one; only fall through to createOrExtendBatch(todayKey) when none exists. Alternatively skip batch creation entirely when the recompute would produce zero gross and absorb nothing.

### M14 — M3 — Nothing ever writes SETTLED, so the stale-payout alarm fires CRITICAL for every SENT batch, every day, forever

- **alan:** minors · **iş yükü:** small

**Kanıt:** settlement-payout.service.ts:246-270 — reconcileStuckBatches still selects every `{ status: 'SENT', sentAt: { lte: now-3d } }` with no upper bound and logs one CRITICAL per batch per run. `grep -rn SETTLED backend/src --include=*.ts` (non-spec) finds SETTLED only in comments, in read-side status filters, and in SETTLEMENT_TRANSITIONS (settlement-transitions.ts:46) — no writer. The set is monotonically growing. Compounds with M2: every empty HELD batch M2 mints also lands in the sibling `overdueUnsent` branch (:271-284) once its dueAt passes.

**Düzeltme taslağı:** Bound the staleSent query with a lower bound too (`sentAt: { lte: now-3d, gte: now-30d }`) and collapse the per-batch loop into one aggregate line carrying the count plus the oldest N ids — one log line per run instead of N. Ideally also add the manual admin `SENT -> SETTLED` action so the set can actually drain.

### M15 — M5 — Four controllers put @Actors on methods instead of the class, so a future handler added without it fails open

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** actors.guard.ts:26-33 still returns true when no @Actors metadata is found. push-tokens.controller.ts:22-23, notification-preferences.controller.ts:18-19, notifications/user-location.controller.ts:24-25 (the file moved from modules/users to modules/notifications but is otherwise unchanged) and reservations.controller.ts:29-30 all still have a bare @Controller with only method-level @Actors. Latent, not currently exploitable — every existing handler declares one.

**Düzeltme taslağı:** Add `@Actors("CONSUMER")` at class level on the three `me/*` controllers (push-tokens, notification-preferences, user-location); getAllAndOverride([handler, class]) means any method-level decorator still wins. reservations.controller.ts is genuinely mixed — leave it, or make ActorsGuard deny by default and require an explicit @Actors/@Public on every route.

### M16 — M6 — Public holidays are seeded only through 2027 with no re-seed item in any runbook and no admin CRUD

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** backend/prisma/migrations/ still contains exactly one holiday migration (20260814193500_seed_public_holidays_2026_2027), whose last row is '2027-10-29' (migration.sql:49). public-holiday.service.ts:5-15 still states there is no admin CRUD surface, and getHolidayDateKeys (:24-32) caches whatever rows exist with no freshness check. `grep -i 'holiday|bayram|tatil' docs/operations.md docs/launch-checklist.md` returns zero hits — neither runbook carries the re-seed or the moon-sighting re-verification item.

**Düzeltme taslağı:** Add a dated 'recurring maintenance' item to docs/operations.md (re-verify 2026/2027 bayram dates against Diyanet; seed 2028+ before 2027-10-01), and add a one-line warn in getHolidayDateKeys when the max holiday date is less than ~6 months out, so the gap announces itself in the logs.

### M17 — M7 — membership-renewal cron has no timeZone while its sibling does; the runbook presents both on the same clock

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** settlement-batch-builder.service.ts:215-218 is the only cron with a timeZone. membership-renewal-cron.service.ts:94 is still `@Cron("0 3 * * *", { name: "membership-renewal" }) // 03:00 server time` and settlement-payout.service.ts:246 is still a bare `@Cron("0 9 * * *")`. `grep -rn TZ ops/docker-compose.prod.yml backend/Dockerfile` returns nothing, so the container is UTC. docs/operations.md's cron table still lists '02:00 Europe/Istanbul', '09:00 daily' and '03:00 daily' side by side as if one clock. The intended ordering still accidentally survives (23:00 UTC precedes 03:00 UTC), so no live bug.

**Düzeltme taslağı:** Add `{ timeZone: "Europe/Istanbul" }` to the membership-renewal and settlement-reconciliation @Cron options, and write the timezone into every row of docs/operations.md's cron table rather than only the one row that has it.

### M18 — M8 — README's dev-up.sh walkthrough omits the one step whose absence breaks a clean clone

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** README.md:53-58 still enumerates exactly six steps (env files, infra, npm ci, prisma generate+migrate deploy, seed, start servers). scripts/dev-up.sh:125-127 still performs an unconditional `npm run build -w @kurtar/ui-tokens` + `npm run build -w @kurtar/api-client` between steps 3 and 4, which its own comment (:109-123) identifies as mandatory on a clean clone. The CI workflow makes the same point independently (quality-gates.yml:421-424: 'both packages must be built before any app is typechecked or built').

**Düzeltme taslağı:** Insert the shared-package build as step 4 in README.md's numbered list, worded from the script's own comment: 'builds @kurtar/ui-tokens and @kurtar/api-client — dist/ is gitignored and npm ci does not compile them, so this is not optional on a clean clone.'

### M19 — M9 — merchant-web's pickup list references two CSS custom properties the token bridge never defines

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** apps/merchant-web/src/today/PickupListSection.module.css:27 (`border-bottom: 1px solid var(--color-border)`), :44 and :49 (`color: var(--color-text-secondary)`) are unchanged. apps/merchant-web/src/styles/theme.ts:13-42 buildCssText emits only --color-{primary,secondary,<semantic>,neutral}-*, --space-*, --radius-*, --font-size/--line-height/--font-weight-* — neither name is produced. A repo-wide grep finds those two names only in this one module (and in the stale built dist/assets CSS, which reproduces the same unresolvable vars). So the border shorthand is invalid and drops to none, and the secondary text renders at full --color-neutral-900 strength.

**Düzeltme taslağı:** Substitute the tokens every other module in the app already uses for these roles: `--color-neutral-200` for the row border, `--color-neutral-600` for .customer/.qty/.pickupTime. (The referenced-token lint check is the same follow-up the DeadlineBadge finding asks for.)

### M20 — M10 — The "Manuel teslim" fallback asks the merchant for a value no surface ever shows them

- **alan:** minors · **iş yükü:** small

**Kanıt:** apps/merchant-web/src/i18n/locales/tr/today.json:65-66 still reads 'rezervasyon kimliğini girerek…' / label 'Rezervasyon kimliği'. hooks.ts:92-95 (useManualRedeem) still calls `client.reservations.redeem(reservationId)`, and reservations.controller.ts:113-123 still routes on `:id` only — there is no by-code lookup anywhere in the controller. PickupListSection.tsx:83-105 renders item.code (:85) and uses item.id only as the React key and the per-row button argument, never as visible text. So the typed value the merchant can actually read (the 6-char code) yields RESERVATION_NOT_FOUND.

**Düzeltme taslağı:** Smallest correct fix: delete the manual form — the per-row 'Teslim et' button (PickupListSection.tsx:100-105) already covers every reservation the list returns and needs no typed input. If the fallback is wanted for a reservation not on the current page, add a merchant-scoped `GET /reservations/by-code/:code` and relabel the field 'Rezervasyon kodu'.

### M21 — M11 — Every deadline badge in a queue table is its own aria-live region

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** apps/admin-web/src/components/DeadlineBadge.tsx:67-71 still renders an unconditional `<span … role="status">`. It is used in five list/table contexts — ComplaintsListPage.tsx:176, ReportsPage.tsx:189, SettlementsListPage.tsx:128, DashboardPage.tsx:112 and :144 — plus two legitimate standalone ones (SettlementDetailPage.tsx:222, ComplaintDetailPage.tsx:192). One page render therefore stands up ~20 simultaneous polite live regions.

**Düzeltme taslağı:** Add a `live?: boolean` prop defaulting to false; render `role="status"` only when it is true, and pass `live` from the two detail-page usages only. Text content and styling stay identical.

### M22 — M12 — Hardcoded Turkish strings outside i18n in the consumer app, which otherwise has exact tr/en key parity

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** All four are unchanged: SwipeToConfirm.tsx:90 `accessibilityHint="Teslim almayı onaylamak için etkinleştir"`, LiveClock.tsx:29 `accessibilityLabel={`Canlı saat: …`}`, purchase/[offerId].tsx:120 and :130 `accessibilityLabel="Azalt"` / `"Artır"`, use-order-details.ts:106 `storeName: … ?? "Mağaza"`. Three of the four are the sole screen-reader text on the redeem swipe and the quantity stepper.

**Düzeltme taslağı:** Add four keys to apps/consumer/src/i18n/tr.json and en.json under the existing redeem.*/purchase.*/orders.* namespaces and read them via t(); LiveClock's takes the formatted time as an interpolation.

### M23 — M13 — The consumer app reconstructs the pickup start time from a hand-mirrored backend constant with nothing tying the two together

- **alan:** minors · **iş yükü:** small

**Kanıt:** apps/consumer/src/lib/constants.ts:13-19 still hardcodes `CANCEL_DEADLINE_BEFORE_PICKUP_MS = 2h` and derivePickupStartAt, consumed by OrderRow.tsx:47 and referenced by purchase-cache.ts:10 and offer/[id].tsx:18-20. The backend still does not send the window on the consumer path: reservation-response.dto.ts adds pickupStartAt/pickupEndAt only on ReservationForMerchantItemDto (:94-95, the /reservations/for-merchant shape); ReservationDto (:13) — what GET /reservations/mine returns — still has neither. No shared constant and no cross-workspace assertion exists.

**Düzeltme taslağı:** Add `pickupStartAt`/`pickupEndAt` to ReservationDto (the service already joins the offer), then have OrderRow read them and delete derivePickupStartAt. That also removes the purchase-cache snapshot's reason to exist. Cheaper stopgap: a consumer-side test that asserts the mirrored 2h against the backend constant.

### M24 — M14 — The single-offer endpoint added 'for share links' has zero callers; the share-link page still documents it as missing

- **alan:** minors · **iş yükü:** small

**Kanıt:** landing/app/[locale]/o/[id]/page.tsx:13-22 still carries the stale doc comment ('There is no public "get one offer by id" endpoint in the backend today') and still renders the generic bridge. The endpoint exists — discovery.controller.ts:38 `@Get("offers/:id")` — and packages/api-client/src/domains/discovery.ts:19-21 exposes `discovery.offer(id)` with a doc comment that literally claims 'Used by landing.' A repo-wide grep for `discovery.offer(` across apps/ and landing/ returns zero call sites.

**Düzeltme taslağı:** In OfferBridgePage (a server component already), await `client.discovery.offer(id)` inside a try/catch, render store name + price + pickup window above the app-store CTAs, feed the same data into generateMetadata's og tags, and delete the stale 'no endpoint' paragraph. Fall back to the current generic bridge on 404.

### M25 — M15 — Cross-module stale assumptions: publish-scheduler, CommissionInvoice schema comment, and cron timezones

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** (a) offers-publish-scheduler.service.ts:14-18 still says 'nothing downstream reacts to offer.published.v1 yet (the outbox worker lands in a later task)' — outbox-worker.service.ts:108 and the favourites/nearby fan-out both exist now, so the stated precondition for jitter is met and the comment is false. (b) schema.prisma:1112-1117 still documents MEMBERSHIP invoices as 'the amount offset this batch / 0 / same value — no KDV line', while invoicing/commission-invoice.service.ts:113-127 writes `vatCents: batch.membershipOffsetVatCents` and nets it out of netAmountCents — the schema and the writer disagree about a money column. (c) is the same defect as M7 (bare @Cron timezones) — treat M7 as canonical for that third.

**Düzeltme taslağı:** (a) Rewrite the scheduler comment to state the fan-out now exists, and either add a small per-offer jitter in publishDueScheduled or record the explicit decision not to. (b) Rewrite the schema.prisma comment to describe the current split (netAmountCents = offset minus its VAT portion, vatCents = membershipOffsetVatCents) and cite commission-invoice.service.ts:109-127. (c) see M7.

### M26 — M16 — Small dead ends: unused client methods, an inert preference toggle, and an invoice status no surface shows

- **alan:** minors · **iş yükü:** small

**Kanıt:** All four sub-items hold. (i) packages/api-client/src/domains/merchant.ts:45-46 (`stores.get`) and :60-61 (`bagTemplates.get`) — grep across apps/, landing/, e2e/ finds zero callers. (ii) marketingEnabled is persisted (dto/notification-preference-response.dto.ts:11), defaulted (notification-policy.service.ts:19,110) and rendered as a toggle (apps/consumer/src/app/notification-preferences.tsx:151), but notification-policy.table.ts:36 types preferenceField as only `"favoritesEnabled" | "nearbyEnabled"` and NOTIFICATION_POLICY_TABLE (:39-46) maps no kind to it — consent is captured and never consulted. (iii) commissionInvoices is still included in both settlement detail responses (settlements.service.ts:72) and grep for 'invoice' in apps/admin-web SettlementDetailPage.tsx and apps/merchant-web/src/earnings/*.tsx returns nothing but the merchant test fixture; adapters/mock-e-document-provider.ts:30 still refuses to register when NODE_ENV==='production' and adapters/nilvera.adapter.ts:33 is still hard-disabled, so production invoices stay DRAFT invisibly. (iv) The api-types.ts preamble item is the same defect as M17 — treat M17 as canonical for it. Note (iii) overlaps the separately-tracked I2 (failed e-invoice dead end); the piece unique here is that no surface renders invoice status at all.

**Düzeltme taslağı:** Delete merchant.stores.get and merchant.bagTemplates.get (nothing imports them; regenerating is trivial if ever needed). Hide the marketingEnabled toggle behind the absence of a marketing NotificationKind, or add the kind and widen preferenceField to include it. Render each commissionInvoice's type/status/total in admin-web's SettlementDetailPage and add a DRAFT-invoice count tile to the admin dashboard.

### M27 — M17 — apps/consumer hand-mirrors 261 lines of response DTOs on a stale premise, with no gate keeping them in sync

- **alan:** minors · **iş yükü:** small

**Kanıt:** apps/consumer/src/lib/api-types.ts:1-29 still carries the full 27-line 'CRITICAL bug … SuccessBody<P,M> collapses to never for ALL 81 operations' header, and the file is still 270 lines of hand-written shapes. The premise is demonstrably false now: packages/api-client/src/core-types.ts:93-94 reads `[K in keyof R]: K extends number ? `${K}` extends `2${string}` ? …`, i.e. the numeric-key coercion is present and SuccessBody resolves correctly. Drift risk plus a misleading header that will send the next reader down a dead investigation; the shapes themselves are still accurate.

**Düzeltme taslağı:** Delete the header and replace each interface with an `Awaited<ReturnType<typeof client.<domain>.<method>>>` projection (or an indexed access into it), mirroring what apps/admin-web/src/api/admin-types.ts already does. Typecheck is the gate.

### M28 — M18 — `RequestOptions.signal` is unreachable — no domain method accepts or forwards an AbortSignal

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** packages/api-client/src/engine.ts:26 still declares `signal?: AbortSignal` on RequestOptions and forwards it at :225 into fetchArgs and :166 into fetch, but `grep -rn signal packages/api-client/src/domains/*.ts` returns zero hits — none of the 13 domain modules exposes it, so no app can ever pass one. The rider in the original finding is now moot: engine.ts:180-193 shows the network-failure path rethrows via errorFromNetworkFailure and no longer calls onUnauthorized, so an aborted refresh would no longer log the user out.

**Düzeltme taslağı:** Cheapest honest fix: drop `signal` from RequestOptions so the capability isn't implied. If cancellation is actually wanted, add an optional trailing `opts?: { signal?: AbortSignal }` to the read-heavy domain methods (discovery.offers/offer/store, reservations.mine) and forward it into engine.request.

### M29 — M19 — The money-loop e2e never exercises the shared client

- **alan:** minors · **iş yükü:** trivial

**Kanıt:** The observation still holds literally: e2e/tests/money-loop.spec.ts:127 and :142 still route every backend call through raw `request.post/get(`${API_BASE}${path}`)` helpers with hardcoded path strings (e.g. :401 /api/discovery/offers, :464 /api/webhooks/payment) — no @kurtar/api-client import anywhere in the spec. But the actionable half of the recommendation landed: quality-gates.yml:483-490 now runs `npm test -w @kurtar/api-client` plus the four frontend apps' own suites (commit fe21c13), so the client is no longer uncovered in CI, and the e2e job comment (:589-591) already redirects readers to apps/consumer's jest+RNTL suite for the consumer side. What remains is one clarifying sentence.

**Düzeltme taslağı:** Add one line to the e2e-money-loop job comment in .github/workflows/quality-gates.yml: this job proves the API contract and the built browser surfaces, not @kurtar/api-client — that layer is covered by the frontend-quality job's api-client and per-app suites.

---

## Aksiyon gerekmeyenler

- **[duplicate]** CommissionInvoiceService is the only outbox handler that isn't idempotent, and there is no unique constraint behind it
  - Same defect and same fix as C1's sibling critical, 'Commission invoicing is not idempotent while the outbox is at-least-once — a redelivery issues a second real e-fatura for the same batch'. Both point at backend/src/modules/invoicing/commission-invoice.service.ts:147 (unconditional `commissionInvoi
- **[duplicate]** admin-web throws away the access token its own login returns, so every session depends on an immediate 401→refresh round trip
  - Same defect, same location, same fix as I5 ("admin-web also drops the access token from login…"): both cite apps/admin-web/src/auth/AuthContext.tsx:133 and both prescribe `setStoredAccessToken(result.accessToken)` in `login()`. I25's extra detail (getStoredAccessToken at api/client.ts:21 exported an
- **[already-fixed]** POST /reports has no caller — the whole moderation subsystem can never receive an item
  - Fixed by commit c8addab ("feat(consumer): add a report affordance for offers and stores"). apps/consumer/src/hooks/use-reports.ts:20 calls `client.complaints.createReport(body)`; apps/consumer/src/app/report/new.tsx:26,36-40 submits `{targetType, targetId, reason}` which matches backend/src/modules/
- **[already-fixed]** GET /admin/merchants/{id} — the audited KYC detail read — has no client method and no admin-web caller
  - Fixed by commit 7f9a720. The client method now exists: packages/api-client/src/domains/admin.ts:38-40 `merchants.get(id) → GET /api/admin/merchants/{id}`. admin-web consumes it: apps/admin-web/src/features/merchants/useMerchants.ts:95-102 (`useMerchantDetail`, queryKey ["admin","merchants","detail",
