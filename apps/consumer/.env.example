# Origin of the kurtar API — the client appends /api/... itself, do not
# include a trailing /api here. Expo only inlines env vars prefixed
# EXPO_PUBLIC_ into the bundle (see https://docs.expo.dev/guides/environment-variables/).
EXPO_PUBLIC_API_BASE_URL=http://localhost:4750
