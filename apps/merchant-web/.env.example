# Origin of the kurtar API — the client appends /api/... itself, do not
# include a trailing /api here.
VITE_API_BASE_URL=http://localhost:4750

# Origin of the `landing` app — the published legal documents (Aracılık
# Sözleşmesi, etc.) the onboarding attestation checkboxes link to.
VITE_LANDING_URL=http://localhost:3000
