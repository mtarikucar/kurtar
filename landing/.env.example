# Origin of the kurtar API — the client appends /api/... itself, do not
# include a trailing /api here. NEXT_PUBLIC_ vars are inlined into the
# client bundle at build time.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4750
